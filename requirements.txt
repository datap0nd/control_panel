createOrReplace

	ref table Sales

		column 'Account / Partner'
			dataType: string
			sourceColumn: account_partner
			isHidden
This restores the required binding to the account_partner column. createOrReplace expects a complete, valid definition of the replaced object. Microsoft TMDL documentation
2. Remove the pending logic
In the Sales Power Query, replace the #"Added Account Partner" step with:
#"Added Account Partner" =
    Table.AddColumn(
        #"Added SC+ Plan Type",
        "account_partner",
        each
            if [sub_sales_channel_name] <> null
                and Text.Trim([sub_sales_channel_name]) <> ""
            then Text.Trim([sub_sales_channel_name])
            else null,
        type nullable text
    ),
