Agreed. Each Region / Operator test run will produce approximately five screenshots, one per stage. The filename itself will be the source identifier.

This also means stage must be added to the mapping CSV and SQL table.

REVISED MESSAGE TO T AND S

Hey T and S, I’m defining the input for the screenshot-validation process and need to confirm what the screenshot process will provide.

For each Region / Operator combination, we will capture approximately five different stages. For now, we can call them Stage A, Stage B, and so on.

For each screenshot, I need:

- PNG format
- Full screenshot at its original resolution
- Region
- Operator
- Stage
- Capture date and time

I’m proposing this filename format:

Region__Operator__Stage-A__YYYYMMDD_HHMMSS.png

Examples:

Region-1__Operator-1__Stage-A__20260720_165748.png
Region-1__Operator-1__Stage-B__20260720_165752.png

The filename will be the identifier, so a separate Capture ID is not required.

Can you confirm whether this format works with the screenshot process? If you already have a format, please send me two or three exact examples covering different stages.

I also need to understand what happens when screenshot capture fails. Is no file created, or is some kind of failure file produced?

Finally, please send me the path to the permanent shared project folder where the PNGs will be saved, plus two or three sample PNGs so I can test the technical ingestion.

I do not need the samples classified as good or bad.

UPDATED NAMING CONTRACT

Region__Operator__Stage__YYYYMMDD_HHMMSS.png

The filename is unique provided the screenshot process does not create two screenshots for the same Region, Operator, and Stage within the same second.

REQUIRED DOWNSTREAM CHANGES

The mapping CSV becomes:

region,operator,stage,prompt_file,prompt_version,enabled

The SQL table must also include:

stage

Each test run will generate approximately five rows per Region / Operator, one row for each stage.
