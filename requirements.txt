EVALUATE
ROW (
    "Policy rows", CALCULATE ( COUNTROWS ( Policy ), REMOVEFILTERS () ),
    "Paid-monthly rows", CALCULATE ( COUNTROWS ( Policy ), REMOVEFILTERS (), Policy[Product Monthly Amount] > 0 ),
    "Minimum created date", CALCULATE ( MIN ( Policy[Policy Created Date] ), REMOVEFILTERS () ),
    "Maximum created date", CALCULATE ( MAX ( Policy[Policy Created Date] ), REMOVEFILTERS () ),
    "Maximum sales date", CALCULATE ( MAX ( Sales[Reporting Date] ), REMOVEFILTERS () )
)
