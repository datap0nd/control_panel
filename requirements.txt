createOrReplace

	ref table Measures_table

		measure 'KPI Comparison Data End Date' =
				VAR _SalesEnd =
					CALCULATE ( MAX ( Sales[Reporting Date] ), REMOVEFILTERS () )
				VAR _PolicyEnd =
					CALCULATE ( MAX ( Policy[Policy Created Date] ), REMOVEFILTERS () )
				RETURN
					MAX ( _SalesEnd, _PolicyEnd )
			formatString: yyyy-mm-dd
			isHidden
			displayFolder: Helper

		measure 'KPI Comparison Period Start Date' =
				VAR _DataEnd = [KPI Comparison Data End Date]
				VAR _HasDateContext = [Policy Date Context Flag] = 1
				VAR _SelectedStart = MIN ( 'Date'[Date] )
				RETURN
					IF (
						ISBLANK ( _DataEnd ),
						BLANK (),
						IF ( _HasDateContext, _SelectedStart, DATE ( YEAR ( _DataEnd ), 1, 1 ) )
					)
			formatString: yyyy-mm-dd
			isHidden
			displayFolder: Helper

		measure 'KPI Comparison Period End Date' =
				VAR _DataEnd = [KPI Comparison Data End Date]
				VAR _HasDateContext = [Policy Date Context Flag] = 1
				VAR _SelectedEnd = MAX ( 'Date'[Date] )
				RETURN
					IF (
						ISBLANK ( _DataEnd ),
						BLANK (),
						IF ( _HasDateContext, MIN ( _SelectedEnd, _DataEnd ), _DataEnd )
					)
			formatString: yyyy-mm-dd
			isHidden
			displayFolder: Helper

		measure 'CY Month Scope Sales Volume' =
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Sales Volume],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0
			isHidden
			displayFolder: Helper

		measure 'LY Month Scope Sales Volume' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -12 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -12 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Sales Volume],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0
			isHidden
			displayFolder: Helper

		measure 'LY-1 Month Scope Sales Volume' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -24 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -24 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Sales Volume],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0
			isHidden
			displayFolder: Helper

		measure 'Gross Revenue CY' =
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Gross Revenue],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Gross Revenue LY Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -12 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -12 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Gross Revenue],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Gross Revenue LY-1 Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -24 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -24 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Gross Revenue],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Net Revenue CY' =
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Net Revenue / Program Margin],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Net Revenue LY Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -12 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -12 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Net Revenue / Program Margin],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Net Revenue LY-1 Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -24 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -24 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Net Revenue / Program Margin],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: #,##0.00
			displayFolder: Chart aliases

		measure 'Attach Rate CY' =
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Raw Attach Rate],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: 0.0%
			displayFolder: Chart aliases

		measure 'Attach Rate LY Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -12 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -12 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Raw Attach Rate],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: 0.0%
			displayFolder: Chart aliases

		measure 'Attach Rate LY-1 Chart' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -24 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -24 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Raw Attach Rate],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: 0.0%
			displayFolder: Chart aliases

		measure 'Profit % CY' =
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Program Margin Rate],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: 0.0%
			displayFolder: Chart aliases

		measure 'Profit % LY' =
				VAR _PeriodStart = EDATE ( [KPI Comparison Period Start Date], -12 )
				VAR _PeriodEnd = EDATE ( [KPI Comparison Period End Date], -12 )
				RETURN
					IF (
						ISBLANK ( _PeriodStart ) || ISBLANK ( _PeriodEnd ) || _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							[Program Margin Rate],
							REMOVEFILTERS ( 'Date' ),
							REMOVEFILTERS ( 'Date Filter' ),
							DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
						)
					)
			formatString: 0.0%
			displayFolder: Chart aliases

		measure 'Sales Plan QTY' =
				VAR _SourceType = SELECTEDVALUE ( 'Source'[Source Type] )
				VAR _UnsupportedDetailFilter =
					ISFILTERED ( Sales[Device Category] )
						|| ISFILTERED ( Sales[Device Model] )
						|| ISFILTERED ( Sales[Product Type] )
						|| ISFILTERED ( Sales[Payment Type] )
						|| ISFILTERED ( Sales[SC+ Plan Rollup] )
						|| ISFILTERED ( Sales[SC+ Plan Type] )
						|| ISFILTERED ( Sales[Account / Partner] )
						|| ISINSCOPE ( Sales[Device Category] )
						|| ISINSCOPE ( Sales[Device Model] )
						|| ISINSCOPE ( Sales[Product Type] )
						|| ISINSCOPE ( Sales[Payment Type] )
						|| ISINSCOPE ( Sales[SC+ Plan Rollup] )
						|| ISINSCOPE ( Sales[SC+ Plan Type] )
						|| ISINSCOPE ( Sales[Account / Partner] )
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				VAR _MonthKeys =
					CALCULATETABLE (
						VALUES ( 'Date'[YearMonth] ),
						REMOVEFILTERS ( 'Date' ),
						REMOVEFILTERS ( 'Date Filter' ),
						DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
					)
				VAR _SelectedSalesChannels =
					CALCULATETABLE (
						VALUES ( Sales[Sales Channel] ),
						REMOVEFILTERS ( 'Date' ),
						REMOVEFILTERS ( 'Date Filter' )
					)
				RETURN
					IF (
						_SourceType IN { "Revenue", "Policy" }
							|| _UnsupportedDetailFilter
							|| ISBLANK ( _PeriodStart )
							|| ISBLANK ( _PeriodEnd )
							|| _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							SUM ( 'Sales Plan'[plan_value] ),
							'Sales Plan'[Sales Plan Type] = "Sales",
							TREATAS ( _MonthKeys, 'Sales Plan'[Month Key] ),
							TREATAS ( VALUES ( 'Subsidiary Master'[Subsidiary] ), 'Sales Plan'[Subsidiary] ),
							TREATAS ( _SelectedSalesChannels, 'Sales Plan'[Sales Channel] )
						)
					)
			formatString: #,##0
			displayFolder: Sales Plan and Revenue Plan

		measure 'Revenue Plan Amount' =
				VAR _SourceType = SELECTEDVALUE ( 'Source'[Source Type] )
				VAR _UnsupportedDetailFilter =
					ISFILTERED ( Sales[Sales Channel] )
						|| ISFILTERED ( Sales[Device Category] )
						|| ISFILTERED ( Sales[Device Model] )
						|| ISFILTERED ( Sales[Product Type] )
						|| ISFILTERED ( Sales[Payment Type] )
						|| ISFILTERED ( Sales[SC+ Plan Rollup] )
						|| ISFILTERED ( Sales[SC+ Plan Type] )
						|| ISFILTERED ( Sales[Account / Partner] )
						|| ISINSCOPE ( Sales[Sales Channel] )
						|| ISINSCOPE ( Sales[Device Category] )
						|| ISINSCOPE ( Sales[Device Model] )
						|| ISINSCOPE ( Sales[Product Type] )
						|| ISINSCOPE ( Sales[Payment Type] )
						|| ISINSCOPE ( Sales[SC+ Plan Rollup] )
						|| ISINSCOPE ( Sales[SC+ Plan Type] )
						|| ISINSCOPE ( Sales[Account / Partner] )
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				VAR _MonthKeys =
					CALCULATETABLE (
						VALUES ( 'Date'[YearMonth] ),
						REMOVEFILTERS ( 'Date' ),
						REMOVEFILTERS ( 'Date Filter' ),
						DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
					)
				RETURN
					IF (
						_SourceType = "Policy"
							|| _UnsupportedDetailFilter
							|| ISBLANK ( _PeriodStart )
							|| ISBLANK ( _PeriodEnd )
							|| _PeriodStart > _PeriodEnd,
						BLANK (),
						CALCULATE (
							SUM ( 'Revenue Plan'[plan_value] ),
							TREATAS ( _MonthKeys, 'Revenue Plan'[Month Key] ),
							TREATAS ( VALUES ( 'Subsidiary Master'[Subsidiary] ), 'Revenue Plan'[Subsidiary] )
						)
					)
			formatString: #,##0.00
			displayFolder: Sales Plan and Revenue Plan

		measure 'Sales Plan Attach Rate' =
				VAR _SourceType = SELECTEDVALUE ( 'Source'[Source Type] )
				VAR _UnsupportedDetailFilter =
					ISFILTERED ( Sales[Device Category] )
						|| ISFILTERED ( Sales[Device Model] )
						|| ISFILTERED ( Sales[Product Type] )
						|| ISFILTERED ( Sales[Payment Type] )
						|| ISFILTERED ( Sales[SC+ Plan Rollup] )
						|| ISFILTERED ( Sales[SC+ Plan Type] )
						|| ISFILTERED ( Sales[Account / Partner] )
						|| ISINSCOPE ( Sales[Device Category] )
						|| ISINSCOPE ( Sales[Device Model] )
						|| ISINSCOPE ( Sales[Product Type] )
						|| ISINSCOPE ( Sales[Payment Type] )
						|| ISINSCOPE ( Sales[SC+ Plan Rollup] )
						|| ISINSCOPE ( Sales[SC+ Plan Type] )
						|| ISINSCOPE ( Sales[Account / Partner] )
				VAR _PeriodStart = [KPI Comparison Period Start Date]
				VAR _PeriodEnd = [KPI Comparison Period End Date]
				VAR _MonthKeys =
					CALCULATETABLE (
						VALUES ( 'Date'[YearMonth] ),
						REMOVEFILTERS ( 'Date' ),
						REMOVEFILTERS ( 'Date Filter' ),
						DATESBETWEEN ( 'Date'[Date], _PeriodStart, _PeriodEnd )
					)
				VAR _SelectedSalesChannels =
					CALCULATETABLE (
						VALUES ( Sales[Sales Channel] ),
						REMOVEFILTERS ( 'Date' ),
						REMOVEFILTERS ( 'Date Filter' )
					)
				VAR _PlanGrain =
					CALCULATETABLE (
						SUMMARIZE (
							'Sales Plan',
							'Sales Plan'[Month Key],
							'Sales Plan'[Subsidiary],
							'Sales Plan'[Sales Channel]
						),
						TREATAS ( _MonthKeys, 'Sales Plan'[Month Key] ),
						TREATAS ( VALUES ( 'Subsidiary Master'[Subsidiary] ), 'Sales Plan'[Subsidiary] ),
						TREATAS ( _SelectedSalesChannels, 'Sales Plan'[Sales Channel] )
					)
				VAR _WeightedNumerator =
					SUMX (
						_PlanGrain,
						VAR _PlanUnits =
							CALCULATE (
								SUM ( 'Sales Plan'[plan_value] ),
								'Sales Plan'[Sales Plan Type] = "Sales"
							)
						VAR _PlanRate =
							CALCULATE (
								AVERAGE ( 'Sales Plan'[plan_value] ),
								'Sales Plan'[Sales Plan Type] = "Att"
							)
						RETURN
							_PlanUnits * _PlanRate
					)
				VAR _PlanUnitsDenominator =
					SUMX (
						_PlanGrain,
						CALCULATE (
							SUM ( 'Sales Plan'[plan_value] ),
							'Sales Plan'[Sales Plan Type] = "Sales"
						)
					)
				RETURN
					IF (
						_SourceType = "Policy"
							|| _UnsupportedDetailFilter
							|| ISBLANK ( _PeriodStart )
							|| ISBLANK ( _PeriodEnd )
							|| _PeriodStart > _PeriodEnd,
						BLANK (),
						DIVIDE ( _WeightedNumerator, _PlanUnitsDenominator )
					)
			formatString: 0.0%
			displayFolder: Sales Plan and Revenue Plan
