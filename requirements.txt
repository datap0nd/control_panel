SCREENSHOT VALIDATION - FLOWCHART, TASKS, AND READY-TO-SEND MESSAGES


FLOWCHART

SETUP FLOW

[J requests the criteria from E]
                 |
                 v
[E obtains criteria for every Region, Operator, and Stage]
                 |
                 v
[N creates the prompt files and completes the mapping CSV]
                 |
                 v
[Prompt configuration is ready]


DAILY SCREENSHOT FLOW

[T and S capture about five Stages per Region and Operator]
                 |
                 v
[Save each PNG using the status-first filename convention]
                 |
                 v
[PNG becomes available in the permanent shared folder]
                 |
                 v
[Daily process discovers a new PNG]
                 |
                 v
[Parse Status, Region, Operator, Stage, and timestamp]
                 |
                 v
        [Is Status capturefailed?]
                 |
       +---------+---------+
       |                   |
      YES                  NO
       |                   |
       v                   v
[Skip prompt and       [Status is capture]
 model evaluation]            |
       |                       v
       |              [Find N's prompt using
       |               Region, Operator, Stage]
       |                       |
       |                       v
       |              [Evaluate the screenshot]
       |                       |
       |              +--------+--------+
       |              |                 |
       |              v                 v
       |        [Pass or fail]    [Technical error]
       |              |                 |
       +--------------+-----------------+
                      |
                      v
[Write one historical row to bi_reporting.screenshot_evaluations]
                      |
                      v
[F receives the updated full-history table daily]


IMPORTANT STATUS RULES

Status is the first filename attribute.

The only accepted Status values are:

- capture
- capturefailed

The filename format is:

Status__Region__Operator__Stage__YYYYMMDD_HHMMSS.png

Examples:

capture__Region-1__Operator-1__Stage-A__20260720_165748.png
capture__Region-1__Operator-1__Stage-B__20260720_165752.png
capturefailed__Region-1__Operator-1__Stage-C__20260720_165801.png

The filename is the screenshot identifier. A separate Capture ID is not required.

There will be approximately five Stages for every Region and Operator combination. This produces approximately five rows for each Region and Operator test run.


MY TASK FLOW

1. Contact T and S to confirm the screenshot format, status handling, filename structure, sample files, timestamp timezone, and permanent shared-folder path.

2. Create the mapping CSV template with these columns:

   region
   operator
   stage
   prompt_file
   prompt_version
   enabled

3. Send the mapping template to N after N receives the criteria requested from E by J.

4. Receive the completed mapping and prompt files from N.

5. Build the daily screenshot-processing flow.

6. Store the full evaluation history in bi_reporting.screenshot_evaluations.

7. Give F the final table structure and confirm whether any additional reporting columns are required.


MESSAGE TO T AND S

Hey T and S,

I’m defining the input for the screenshot-validation process and need to confirm what the screenshot process will provide.

For each Region and Operator combination, we expect to capture approximately five different Stages. For now, we can call them Stage A, Stage B, Stage C, and so on.

For every screenshot, I need:

- PNG format
- Full screenshot at its original resolution
- Status
- Region
- Operator
- Stage
- Capture date and time

Status must be the first part of the filename. The only Status values will be capture and capturefailed.

I’m proposing the following filename format:

Status__Region__Operator__Stage__YYYYMMDD_HHMMSS.png

For example:

capture__Region-1__Operator-1__Stage-A__20260720_165748.png
capture__Region-1__Operator-1__Stage-B__20260720_165752.png
capturefailed__Region-1__Operator-1__Stage-C__20260720_165801.png

The filename will be the screenshot identifier, so I do not need a separate Capture ID.

Can you confirm whether this filename format works with the screenshot process? If you already have a format in place, please send me two or three exact filename examples covering different Stages.

I also need to understand what happens when capture fails. Is no file created, or is a failure file or record produced? The daily process can only record capturefailed if an artifact is available for it to detect. If a failure artifact is produced, please send me an example of its filename and format.

Please also send me:

- The path to the permanent shared project folder where the PNGs will be saved
- Two or three sample PNGs covering different Stages so I can test the technical ingestion
- Confirmation of the timezone used in the filename timestamp

The sample PNGs do not need to be classified as good or bad. I only need them to verify that the process can find, read, and send the images correctly.


MESSAGE TO N

Hi N,

J has already asked E to obtain the criteria for each Region, Operator, and Stage combination.

Once you receive those criteria, I need the final prompt configuration for the screenshot-validation process.

I will create and send you a mapping CSV template containing these columns:

region
operator
stage
prompt_file
prompt_version
enabled

I need you to:

1. Confirm which prompt applies to each Region, Operator, and Stage combination.

2. Create one prompt criteria file for each distinct set of criteria. The same prompt file can be reused where the criteria are identical.

3. Populate the mapping CSV with the correct prompt file for every required combination.

4. Set the initial approved prompts to version 1.

5. Increase the prompt version whenever the criteria change.

6. Confirm when the prompt files and mapping are ready for production use.

My code will add the technical instructions requiring a structured response containing a pass or fail result and a reason. Your prompt files should contain the actual business criteria used to decide whether the screenshot passes or fails.

Status is not part of the prompt mapping. Files with Status capture will use Region, Operator, and Stage to select a prompt. Files with Status capturefailed will skip prompt selection and visual validation.

Labelled pass and fail screenshots are not production inputs to my process. If you need examples to test and validate the prompt quality, please coordinate those with E. I only need the final prompt files and the completed mapping.

Please flag any combination for which the criteria are missing or unclear instead of creating assumptions inside the prompt.


EMAIL TO F

Subject: Proposed screenshot evaluation table

Hi F,

I am defining the output table for the screenshot-validation process.

The table will be:

bi_reporting.screenshot_evaluations

The process will update the table daily.

I propose keeping the full evaluation history. Existing results will not be overwritten. If a screenshot is evaluated again with a different prompt version or model version, it will create a new evaluation row.

Each row will represent one screenshot evaluation for a specific Region, Operator, and Stage.

The proposed columns are:

- evaluation_id: Unique identifier for the evaluation
- evaluation_key: Prevents the same evaluation from being created twice
- screenshot_id: Complete source filename without the .png extension
- source_filename: Complete source filename including the .png extension
- image_sha256: Screenshot fingerprint used for audit and integrity checks
- capture_status: capture or capturefailed, taken from the first filename attribute
- region: Region from the screenshot filename
- operator: Operator from the screenshot filename
- stage: Screenshot stage, such as Stage A or Stage B
- captured_at: Date and time when the screenshot was captured
- processing_status: Indicates whether technical processing succeeded
- validation_result: Pass, fail, or blank when no valid visual result exists
- validation_reason: Reason provided for the pass or fail result
- prompt_version: Version of the prompt used for the evaluation
- model_id: Model version used for the evaluation
- attempt_count: Number of processing attempts
- error_code: Technical error category
- error_message: Technical error details
- created_at: Date and time when the evaluation row was created
- completed_at: Date and time when processing finished

Technical errors will be stored in the same table. They will be recorded as follows:

processing_status = error
validation_result = blank
error_code = populated
error_message = populated

This ensures that a technical processing problem is not incorrectly reported as a visual failure.

If screenshot capture fails before visual validation, the row will be recorded as follows:

capture_status = capturefailed
processing_status = skipped
validation_result = blank

Raw model responses will not be stored in the reporting table.

Please confirm whether you require any additional columns beyond those listed above.

Thanks.
